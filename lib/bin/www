#!/usr/bin/env node

"use strict";

var debug  = require("debug")("rna_central");
var server = require("../app");
var config = require("config");

var server = server.listen(config.get("port"), function() {
  debug("Express server listening on port %s", server.address().port);
  debug("Running Node %s", process.version);
});
