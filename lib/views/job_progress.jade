extends layout

block content
  #main
    .header
      h1= webserver.title
      h2 Job Progress
    .content
      h2.content-subhead= job.nickname
      pre
        code.json= JSON.stringify(job, null, 2)
      pre
        code.json= JSON.stringify(session, null, 2)
